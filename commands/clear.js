module.exports = {
    name: "clear",
    description: "",

    options: [
        {
            name: "query",
            type: "NUMBER",
            description: "amount of messages to clear",
            required: true
        }
    ],

    run: async (client, message, args, slash) => {
        
    }
}